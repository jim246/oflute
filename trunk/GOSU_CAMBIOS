GosuImpl/Graphics/TextUnix.cpp
L: 233, antes de bmp.insert(temp, x, y);
if(bmp.height() < temp.height()){
    bmp.resize(bmp.width(), temp.height());
}


Font.cpp
- Remove * 2 from:
  pimpl -> height = fontHeight * 2;

- Remove / 2 from:
  return pimpl -> height / 2;

- Remove / 2 from:
  return result * factorX / 2;

- Change 0.5 to 1 at:
  curfactor = 0.5,

- Change 0.5 to 1 at impl::factorAt
